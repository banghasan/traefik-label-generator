## Docker Compose

### Deskripsi

Berisi file docker compose sesuai service nama yang dipakai.

### Menjalankan

Pastikan docker compose sudah diinstal

```bash
docker compose -f namafile.yml up -d
```
